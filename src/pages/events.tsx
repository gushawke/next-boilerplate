import { Typography } from 'antd';

const { Title, Paragraph } = Typography;

const Events = () => {
  return (
    <div style={{ padding: '20px' }}>
      <Title>Events</Title>
      <Paragraph>
        This is the Events page. Check out our latest events here.
      </Paragraph>
    </div>
  );
};

export default Events;
